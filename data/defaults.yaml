lookup_options:
  mcollective_agent_iptables::gem_dependencies:
    merge:
      strategy: deep
  mcollective_agent_iptables::package_dependencies:
    merge:
      strategy: deep
  mcollective_agent_iptables::config:
    merge:
      strategy: deep
  mcollective_agent_iptables::client_config:
    merge:
      strategy: deep
  mcollective_agent_iptables::server_config:
    merge:
      strategy: deep
